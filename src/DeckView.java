import javax.swing.*;

public class DeckView extends JPanel {
    private Deck deck;

    public DeckView(Deck deck) {
        this.deck = deck;
    }

    public void update() {
        // Logic to update the deck view, e.g., refresh the display of cards in the deck
        // This could involve repainting the component or updating a list of cards
    }
}
